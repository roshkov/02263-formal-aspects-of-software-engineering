Basics

scheme Design =
extend Basics with 
class
  value

    addPerson : (Table >< Person) -> Table
    addPerson(t, p) is t union {p},

    plan : Families -~-> Plan
    plan(fs) is {} 
    post isCorrectPlan(p, fs)
    pre isWellformed(fs),

end